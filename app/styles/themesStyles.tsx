import { StyleSheet } from 'react-native';

const difficultyStyle = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
        backgroundColor: 'white',
        flexDirection: 'row',
        flexWrap: 'wrap',
        alignContent: 'center',
        gap: 10,
    }
});

export default difficultyStyle;