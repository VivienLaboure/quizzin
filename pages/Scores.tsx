import { View, Text } from "react-native";
import MyStyles from "../constants/MyStyles";

const Scores = () => {
    return (
        <View style={MyStyles.container}>
            <Text style={MyStyles.title}>Scores Page</Text>
        </View>
    );
}
export default Scores;